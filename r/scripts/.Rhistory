#Importando os dados
tabela = read.csv("../data/Exam_Score_Prediction.csv")
#Importando os dados
tabela = read.csv("/data/Exam_Score_Prediction.csv")
#Importando os dados
tabela = read.csv(".../data/Exam_Score_Prediction.csv")
#Importando os dados
tabela = read.csv("..analise-educacao/data/Exam_Score_Prediction.csv")
#Importando os dados
tabela = read.csv("../analise-educacao/data/Exam_Score_Prediction.csv")
#Importando os dados
tabela = read.csv("analise-educacao/data/Exam_Score_Prediction.csv")
#Importando os dados
tabela = read.csv("../data/Exam_Score_Prediction.csv")
#Importando os dados
tabela = read.csv("../../data/Exam_Score_Prediction.csv")
head(tabel
#Importando os dados
tabela = read.csv("../../data/Exam_Score_Prediction.csv")
head(tabela)
#Importando os dados
tabela = read.csv("../../data/Exam_Score_Prediction.csv")
head(tabela,10)
modelo = lm(exam_score ~ age + study_hours + class_attendance + sleep_hours, data = tabela)
summary(modelo)
modelo = lm(exam_score ~ age + study_hours + class_attendance + sleep_hours)
modelo = lm(exam_score ~ age + study_hours + class_attendance + sleep_hours data = tabela)
modelo = lm(exam_score ~ age + study_hours + class_attendance + sleep_hours, data = tabela)
summary(modelo)
